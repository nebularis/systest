{application, systest,
 [
  {description, ""},
  {vsn, {cmd, "git describe --abbrev=0 `git log stable --oneline -1 --format=\"%h\"` | sed 's/v//g'"}},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib
                 ]},
  {mod, { systest_app, []}},
  {env, []}
 ]}.
